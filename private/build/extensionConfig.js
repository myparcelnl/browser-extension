/* eslint-disable no-magic-numbers */

/**
 * This config will be exposed as a json file in the dist folder.
 *
 * @type {{extensionPath: string, popupDimensions: {width: number, height: number}}}
 */
export const extensionConfig = {
  extensionPath: 'browser-extension/create-shipment',
  popupDimensions: {
    width: 500,
    height: 620,
  },
};
